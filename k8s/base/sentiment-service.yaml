apiVersion: v1
kind: Service
metadata:
  name: sentiment-app
  labels:
    app: sentiment-app
spec:
  type: NodePort
  selector:
    app: sentiment-app
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080
